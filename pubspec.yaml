name: unbounded
description: A simple snake game... maybe?
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  equatable: ^2.0.2
  google_fonts: ^2.1.0
  hive: ^2.0.4
  meta: ^1.3.0
  redux: ^5.0.0
  sensors_plus: ^1.0.1
  vector_math: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  effective_dart: ^1.3.0
  test: ^1.16.8

flutter:
  uses-material-design: true
